name: Imagin that it is build from sing-box

on:
  workflow_dispatch:


jobs:
  executor:
    runs-on: ubuntu-latest
    steps:
      - name: BUILD TREEEEEEEEEEEEEEEEE
        run: |
          tree -a